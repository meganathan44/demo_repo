def calculate_sum a, b  # Missing colon
print("Sum is" a + b)   # Undefined variable 'a', missing comma
    result = 10 / 0     # ZeroDivisionError
x = "hello" + 5        # TypeError (string + int)
if x == 5              # Missing colon
    print(x            # Unclosed parenthesis
def multiply(x y)      # Invalid function syntax (missing comma)
    return x * y      # Indentation error (should align with def)
z = undefined_var      # NameError
print("End"            # Unclosed quotation
